/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Keep this empty unless you know you need something.
  },
};

export default nextConfig;
